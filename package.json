{
  "name": "fuken.xyz",
  "version": "6.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "rm -rf build && npm run build && cp build/index.html build/200.html &&  surge build fuken.xyz"
  },
  "devDependencies": {
    "surge": "^0.19.0"
  }
}
